export function Footer() {
  return (
    <footer className="mt-16 border-t border-slate-200 bg-white">
      <div className="mx-auto flex max-w-6xl flex-col gap-1 px-4 py-8 text-sm text-slate-500 sm:flex-row sm:items-center sm:justify-between">
        <span>Marketplace — vendors sell, buyers order, Paystack handles payment.</span>
        <span>Payments are verified server-side before an order is marked paid.</span>
      </div>
    </footer>
  );
}
